interface:
  display_name: "Session Condense"
  short_description: "Maintain and lint local memex"
  default_prompt: "Use $session-condense to run a maintenance pass that consumes changed paxl sessions and .llm-wiki recall traces, extracts durable memex knowledge units, updates qmd wiki pages, backlinks, memex.graph.json, recall-index, inbox, lint, visualization artifacts, and advances processed cursors."
