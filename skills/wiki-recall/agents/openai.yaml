interface:
  display_name: "Wiki Recall"
  short_description: "Rank recall context and record reuse"
  default_prompt: "Use $wiki-recall to rank relevant context from the local qmd LLM wiki, recall-index, memex.graph.json, backlinks, and query trails, then write a recall trace before falling back to raw session search."
